pipeline { 
    agent any 
    stages {
        stage('Build') { 
            steps { 
                withMaven(maven : 'maven_install' )
                {
                sh 'mvn clean'
                }
            }
        }
        stage('Build') { 
            steps { 
                withMaven(maven : 'maven_install' )
                {
                sh 'mvn clean'
                }
            }
        }
        stage('Build') { 
            steps { 
                withMaven(maven : 'maven_install' )
                {
                sh 'mvn clean'
                }
            }
        }
	}
}
